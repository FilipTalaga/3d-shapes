Add `getKeyDown` and `getKeyUP` for the input controller

Make the keys configurable, and use the activity name, instead of key name

Introduce game object with configurable components:

- render (sprite)
- rigid body (physics)
- collider
- animation
- scripts (custom behaviours)
